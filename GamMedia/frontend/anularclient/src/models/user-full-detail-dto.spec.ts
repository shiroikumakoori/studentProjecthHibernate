import { UserFullDetailDTO } from './user-full-detail-dto';

describe('UserFullDetailDTO', () => {
  it('should create an instance', () => {
    expect(new UserFullDetailDTO()).toBeTruthy();
  });
});
